---
interface:
  display_name: "Codex Session Controller"
  short_description: "Coordinate Codex tasks within the user's chosen scope"
  default_prompt: >-
    Use $codex-session-controller to establish or operate a Codex controller
    within the user's chosen scope, perform a one-shot session operation, or
    hand off an existing controller.
