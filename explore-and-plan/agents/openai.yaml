---
interface:
  display_name: "Explore and Plan"
  short_description: "Turn a decided direction into an executable plan"
  default_prompt: "Use $explore-and-plan to create an executable plan."

policy:
  allow_implicit_invocation: false
